(function(a, b){
    console.log("Ejecutando la funcion " + (a + b));
})(4, 7);//aqui recibe el valor de los parámetros
//Este tipo de declaracion de funciones se llama a si misma una sola vez, 
//y ya no puede ser reutilizada, puede servir al momento de que carga el programa.
//No es muy común